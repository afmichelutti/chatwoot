---
phase: 01-foundation-core-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/controllers/api/v1/accounts/conversations_controller.rb
  - app/models/channel/whatsapp.rb
  - app/models/account.rb
  - app/models/contact.rb
  - app/models/conversation.rb
  - app/models/inbox.rb
  - app/models/message.rb
  - app/models/user.rb
  - config/routes.rb
  - config/schedule.yml
autonomous: true
requirements:
  - UPGR-01
  - UPGR-02
  - UPGR-03
  - UPGR-06
  - UPGR-07

must_haves:
  truths:
    - "The git merge of v4.11.1 is executed and all 11 conflict files are identified"
    - "Backend code conflicts (conversations_controller, channel/whatsapp, config) are resolved preserving custom modifications"
    - "The custom PermissionFilterService code in conversations_controller.rb is preserved alongside the upstream authorize changes"
    - "Activity-Based Presence code in account.rb, user.rb, and related files survives the merge"
    - "Corrupted conversation handling in conversation.rb and message.rb survives the merge"
  artifacts:
    - path: "app/controllers/api/v1/accounts/conversations_controller.rb"
      provides: "Merged conversations controller with both upstream authorize fix and custom permission logic preserved"
      contains: "authorize @conversation"
    - path: "app/models/channel/whatsapp.rb"
      provides: "WhatsApp model with custom Cloud API config preserved"
    - path: "config/schedule.yml"
      provides: "Schedule config with custom Activity-Based Presence jobs preserved"
  key_links:
    - from: "app/controllers/api/v1/accounts/conversations_controller.rb"
      to: "app/policies/conversation_policy.rb"
      via: "Pundit authorize call"
      pattern: "authorize @conversation"
    - from: "app/models/account.rb"
      to: "app/jobs/activity_based_presence_job.rb"
      via: "Activity-Based Presence feature flag"
      pattern: "activity_based_presence"
    - from: "app/models/conversation.rb"
      to: "app/models/message.rb"
      via: "Corrupted conversation handling"
      pattern: "corrupted"
    - from: "app/models/user.rb"
      to: "app/models/account.rb"
      via: "Activity-Based Presence user tracking"
      pattern: "activity_based_presence|last_activity_at"
---

<objective>
Execute the git merge of upstream v4.11.1 into the v4.7.0-custom branch and resolve all backend/config merge conflicts while preserving every custom modification.

Purpose: This is the foundational step of the upgrade. The merge brings in 309 upstream commits (including security fixes, new features like TikTok channel, and the upstream conversation permission fix). All 11 conflict files must be identified and the backend/config subset resolved in this plan.

Output: A working merge state with backend conflicts resolved. Frontend/branding conflicts are handled in Plan 02.
</objective>

<execution_context>
@C:/Users/afmic/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/afmic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md

Key context for conflict resolution:
- Upstream commit `9898ccee9` changed `authorize @conversation.inbox, :show?` to `authorize @conversation, :show?` in conversations_controller.rb
- Our custom commit `d237b1f42` added PermissionFilterService for conversation access control
- Our custom commit `962cccc5a` added Activity-Based Presence system (account.rb, user.rb, jobs, schedule.yml)
- Our custom commit `7355cf545` added WhatsApp Cloud API config (channel/whatsapp.rb, inboxes API)
- Our custom commit `90584ec15` added white-label branding (logo files, colors.js, mailer templates)

Conflict files identified (from test merge):
1. app/controllers/api/v1/accounts/conversations_controller.rb (CRITICAL - permission logic)
2. app/javascript/dashboard/api/inboxes.js (WhatsApp config)
3. app/javascript/dashboard/i18n/locale/pt_BR/inboxMgmt.json (translations)
4. app/javascript/dashboard/routes/dashboard/settings/inbox/settingsPage/ConfigurationPage.vue (WhatsApp UI)
5. app/javascript/dashboard/store/modules/inboxes.js (WhatsApp store)
6. app/models/channel/whatsapp.rb (WhatsApp model)
7. config/schedule.yml (Activity-Based Presence cron)
8. db/schema.rb (schema merge)
9. public/brand-assets/logo.svg (white-label)
10. public/brand-assets/logo_dark.svg (white-label)
11. theme/colors.js (white-label)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backup branch and execute git merge v4.11.1</name>
  <files>All repository files (merge operation)</files>
  <action>
1. Create a backup branch from the current state:
   ```bash
   git branch v4.7.0-custom-backup
   ```

2. Execute the merge of v4.11.1:
   ```bash
   git merge v4.11.1 --no-commit
   ```
   The merge will pause due to conflicts. This is expected.

3. Capture and log the exact list of conflicted files:
   ```bash
   git diff --name-only --diff-filter=U
   ```
   Expected: 11 files (conversations_controller.rb, inboxes.js, pt_BR/inboxMgmt.json, ConfigurationPage.vue, inboxes.js store, channel/whatsapp.rb, schedule.yml, schema.rb, logo.svg, logo_dark.svg, colors.js)

4. Verify all auto-merged files are clean (no conflict markers):
   ```bash
   git diff --cached --name-only | head -20
   ```

DO NOT commit yet. Conflicts will be resolved in the following task.
  </action>
  <verify>
- `git status` shows "All conflicts fixed but you are still merging" state (after conflicts resolved) OR shows the list of unmerged paths
- `git branch` shows v4.7.0-custom-backup exists
- `git diff --name-only --diff-filter=U` lists the expected conflict files
  </verify>
  <done>Merge initiated, backup branch created, all 11 conflict files identified and cataloged</done>
</task>

<task type="auto">
  <name>Task 2: Resolve backend and config merge conflicts</name>
  <files>
    app/controllers/api/v1/accounts/conversations_controller.rb
    app/models/channel/whatsapp.rb
    config/schedule.yml
    db/schema.rb
  </files>
  <action>
Resolve the following conflict files (backend/config subset). Frontend conflicts handled in Plan 02.

**1. `app/controllers/api/v1/accounts/conversations_controller.rb` (CRITICAL)**

This is the most important conflict. Both sides modified the controller:
- **Upstream** (v4.11.1): Changed `authorize @conversation.inbox, :show?` to `authorize @conversation, :show?` (commit 9898ccee9) and added daily rate limits for outbound emails, throttled agent_last_seen_at, etc.
- **Ours** (v4.7.0-custom): Added custom PermissionFilterService calls for conversation filtering.

Resolution strategy: **Keep BOTH**. Accept all upstream changes (the authorize fix, rate limits, throttling) AND preserve our custom PermissionFilterService logic. Both approaches reinforce security — the upstream `authorize @conversation, :show?` is a Pundit policy check at the controller action level, while our PermissionFilterService handles filtering in list/search operations. They are complementary, not conflicting.

Specifically:
- Accept the upstream `authorize @conversation, :show?` in show/update actions
- Preserve any custom `PermissionFilterService` calls in index/search/filter actions
- Accept all other upstream additions (rate limiting, throttling, etc.)
- Read both versions carefully (ours and theirs) before resolving — do NOT blindly pick one side

**2. `app/models/channel/whatsapp.rb`**

- **Upstream**: May have refactored or added new WhatsApp features
- **Ours**: Added custom Cloud API configuration management fields/methods

Resolution: Accept upstream changes and merge our custom config methods on top. Our custom additions are additive (new methods), not overrides of existing methods, so they should coexist cleanly.

**3. `config/schedule.yml`**

- **Upstream**: Updated/added scheduled jobs
- **Ours**: Added `activity_based_presence_job` and `update_agent_presence_job` entries

Resolution: Accept all upstream schedule entries and ensure our custom job entries are preserved.

**4. `db/schema.rb`**

- **Upstream**: Added 15+ new migrations (companies, tiktok, webhooks name, etc.)
- **Ours**: Added `activity_based_presence` and `auto_offline` columns to accounts

Resolution: Accept the upstream schema.rb entirely. Our custom migration files (20250118000000, 20250118000001) are separate files that will be re-applied when `rails db:migrate` runs in Plan 03. The schema.rb will be regenerated correctly after migrations. For now, accept theirs for schema.rb.

After resolving each file:
```bash
git add <resolved-file>
```

Do NOT resolve frontend files (inboxes.js, pt_BR json, ConfigurationPage.vue, inboxes.js store) or branding files (logo.svg, logo_dark.svg, colors.js) — those are in Plan 02.
  </action>
  <verify>
- For each resolved file, confirm no conflict markers remain: `grep -rn '<<<<<<' <file>` returns nothing
- `git diff --cached -- app/controllers/api/v1/accounts/conversations_controller.rb` shows the merged result includes both `authorize @conversation` and any PermissionFilterService references
- `config/schedule.yml` still contains `activity_based_presence` entries
- `db/schema.rb` has no conflict markers
- `git diff --name-only --diff-filter=U` still shows only the frontend/branding files as unresolved
  </verify>
  <done>Backend conflicts resolved: conversations_controller has both upstream authorize and custom permission logic, whatsapp.rb preserves custom config, schedule.yml preserves custom jobs, schema.rb accepts upstream version for migration re-application</done>
</task>

<task type="auto">
  <name>Task 3: Verify custom code survived in auto-merged files</name>
  <files>
    app/models/conversation.rb
    app/models/message.rb
    app/models/account.rb
    app/models/user.rb
  </files>
  <action>
Several model files are auto-merged by git (not in the conflict list) but contain critical custom code. Verify that these auto-merged results are correct.

**1. Corrupted conversation handling (UPGR-06)**

Check that our custom corrupted conversation code survived in conversation.rb and message.rb:
```bash
grep -n 'corrupted\|corrupted_conversation\|mark_as_corrupted\|is_corrupted' app/models/conversation.rb
grep -n 'corrupted\|corrupted_conversation\|skip_corrupted\|is_corrupted' app/models/message.rb
```

If the grep returns empty (custom code missing), the auto-merge dropped our modifications. In that case:
- Use `git log --all --oneline -- app/models/conversation.rb` to find our custom commit
- Cherry-pick or manually re-add the corrupted conversation handling code
- The custom code was added to handle conversations that entered an invalid state

**2. Activity-Based Presence (UPGR-03)**

Check that our Activity-Based Presence code survived in account.rb and user.rb:
```bash
grep -n 'activity_based_presence' app/models/account.rb
grep -n 'activity_based_presence\|last_activity_at\|update_activity' app/models/user.rb
```

If the grep returns empty, the auto-merge dropped our modifications. In that case:
- Use `git log --all --oneline -- app/models/account.rb` to find our custom commit (962cccc5a)
- Cherry-pick or manually re-add the Activity-Based Presence code

**3. Read both files to visually confirm custom code is intact**

Read `app/models/conversation.rb` and `app/models/message.rb` to verify the custom code blocks are present and syntactically correct (not truncated or garbled by auto-merge).

If any custom code is missing or garbled:
- Restore from the backup branch: `git show v4.7.0-custom-backup:app/models/conversation.rb`
- Manually merge the custom sections back in
- `git add` the fixed file
  </action>
  <verify>
- `grep -c 'corrupted' app/models/conversation.rb` returns at least 1 (corrupted conversation handling present)
- `grep -c 'corrupted' app/models/message.rb` returns at least 1 (corrupted message handling present)
- `grep -c 'activity_based_presence' app/models/account.rb` returns at least 1 (presence feature flag present)
- `grep -c 'activity_based_presence\|last_activity_at' app/models/user.rb` returns at least 1 (presence tracking present)
- No syntax errors: `ruby -c app/models/conversation.rb` returns "Syntax OK"
- No syntax errors: `ruby -c app/models/message.rb` returns "Syntax OK"
  </verify>
  <done>All auto-merged model files verified: corrupted conversation handling in conversation.rb and message.rb is intact, Activity-Based Presence code in account.rb and user.rb is intact</done>
</task>

</tasks>

<verification>
- `git branch` includes `v4.7.0-custom-backup`
- Merge is in progress with only frontend/branding conflicts remaining
- `grep -rn '<<<<<<' app/controllers/ app/models/channel/whatsapp.rb config/schedule.yml db/schema.rb` returns zero matches
- `git log --oneline v4.11.1 -1` shows the v4.11.1 commit is an ancestor of the merge
- `grep -c 'corrupted' app/models/conversation.rb` returns at least 1 (UPGR-06)
- `grep -c 'corrupted' app/models/message.rb` returns at least 1 (UPGR-06)
- `grep -c 'activity_based_presence' app/models/account.rb` returns at least 1 (UPGR-03)
- `grep -c 'activity_based_presence' app/models/user.rb` returns at least 1 (UPGR-03)
</verification>

<success_criteria>
- The git merge v4.11.1 is initiated with backup branch
- All 4 backend/config conflict files resolved with no conflict markers
- conversations_controller.rb preserves BOTH upstream authorize fix AND custom PermissionFilterService
- 7 frontend/branding conflict files remain for Plan 02
- No backend code regressions from auto-merged files
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-data/01-01-SUMMARY.md`
</output>
